android {
  signingConfigs {
    release {
      storeFile file(project.property("book_keeper.signing") + ".jks")
      storePassword "123456"
      keyAlias "key0"
      keyPassword "123456"
    }
  }

  buildTypes {
    release {
      signingConfig signingConfigs.release
    }
  }
}
